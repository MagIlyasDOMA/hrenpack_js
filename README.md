# hrenpack_js
hrenpack_js — это универсальная frontend-библиотека на TypeScript/JavaScript, предоставляющая набор удобных утилит, кастомных элементов и функций для решения большинства повседневных задач веб-разработки.

---

## 📦 Установка
```shell
npm install hrenpack_js
```

---

## 🚀 Быстрый старт
```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Пример использования hrenpack_js</title>
    <script type="module">
        import { copyTextToClipboard, getSystemTheme } from 'hrenpack_js';
        
        // Использование функций
        copyTextToClipboard('Привет, мир!');
        console.log('Тема системы:', getSystemTheme());
    </script>
</head>
<body>
    <!-- Использование кастомных элементов -->
    <step-bar current="2">
        <sb-element>Шаг 1</sb-element>
        <sb-element>Шаг 2</sb-element>
        <sb-element>Шаг 3</sb-element>
    </step-bar>

    <ab-num>1500000</ab-num>
    <click-to-copy>Скопируй меня</click-to-copy>
</body>
</html>
```

---

## 📚 Модули и функции

### 🎨 Кастомные HTML-элементы
`<step-bar>` и `<sb-element>` — прогресс-бар для пошаговых форм

`<ab-num>` — сокращённое отображение больших чисел

`<include-html>` — вставка HTML-файлов с поддержкой скриптов

`<click-to-copy>` — элемент для копирования текста по клику

### 📋 Работа с DOM
`getInputCursorPosition()` — получить позицию курсора в input

`copyInputToClipboard()` — скопировать значение input в буфер

`getInputLabel()` — найти label для input

`togglePassword() `— переключение видимости пароля

### 🌐 URL и параметры
`GETParamsManager` — управление GET-параметрами URL

`isAbsoluteUrl()` — проверка абсолютного URL

`combineUrls()` — объединение URL

`getHost()` — получение текущего хоста

### 🍪 Работа с Cookie
`getCookie()` / `setCookie()` / `hasCookie()` — управление cookies

### 🔔 Уведомления
`pushNotification()` — системные уведомления

`HyperTextNotification` — кастомные in-page уведомления

### 📄 Типы и утилиты
`NullStr`, `NullNum`, `Dict`, `StringDict` — вспомогательные типы

`generateUUIDv4()` — генерация UUID v4

`arraysIsEqual()` — сравнение массивов

`escapeHTML()` — экранирование HTML

### 🧩 Стилизация форм
`input_form_control()` — добавление классов к элементам формы

`intToPixel()` — преобразование числа в CSS-пиксели

### 📦 Файлы и компиляция
`downloadTextAsFile()` — скачивание текста как файла

`loadCSS()` — динамическая загрузка CSS

---

## 🔧 Конфигурация TypeScript

Пример `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "moduleResolution": "bundler",
    "strict": true,
    "outDir": "./dist"
  },
  "include": ["src/**/*"]
}
```

---

## 📁 Структура проекта

```text
hrenpack_js/
├── input.ts              # Работа с input-элементами
├── get_element_types.ts  # Вспомогательные функции для элементов
├── classes.ts            # Основные классы (ClickableLinksFactory, GETParamsManager)
├── system.ts             # Системные функции (тема, копирование, редирект)
├── tags.ts               # Кастомные HTML-элементы
├── cookie.ts             # Работа с cookie
├── notifications.ts      # Уведомления
├── types.ts              # Пользовательские типы
├── exceptions.ts         # Кастомные исключения
├── arraywork.ts          # Работа с массивами
├── uuid.ts               # Генерация UUID
├── url.ts                # Работа с URL
├── styles.ts             # Стилизация
├── compiler.ts           # Функции компиляции
├── html.ts               # HTML-утилиты
└── package.json          # Конфигурация npm-пакета
```

---

## 🧪 Примеры использования

### Управление GET-параметрами

```typescript
import { GETParamsManager } from 'hrenpack_js';

const params = new GETParamsManager();
params.set('page', 2);
params.get('page'); // "2"
params.all(); // { page: "2" }
```

### Кастомные уведомления
```typescript
import { HyperTextNotification } from 'hrenpack_js';

const notifier = new HyperTextNotification();
notifier.show('Данные сохранены!', 5);
```

### Сокращение чисел
```html
<ab-num>1234567</ab-num> <!-- Отобразится как "1.23 млн." -->
<ab-num lang="en">1234567</ab-num> <!-- Отобразится как "1.23M" -->
```

### Вставка HTML-файлов
```html
<include-html src="/partials/header.html"></include-html>
```

---

## 🛠 Разработка
#### 1. Клонируйте репозиторий:

```shell
git clone https://github.com/MagIlyasDOMA/hrenpack_js.git
```

#### 2. Установите зависимости:

```shell
npm install
```

#### 3. Скомпилируйте TypeScript:
```shell
npx tsc
```

---

## 📄 Лицензия
__MIT License__. Подробнее в файле [`LICENSE`](https://github.com/MagIlyasDOMA/hrenpack_js/blob/main/LICENSE).

---

## 🐛 Отчёты об ошибках и предложения
Нашли ошибку или есть идея для улучшения?
Создайте issue в [GitHub-репозитории](https://github.com/MagIlyasDOMA/hrenpack_js/).

---

## 👨‍💻 Автор
#### Маг Ильяс DOMA (MagIlyasDOMA)
#### [GitHub](https://github.com/MagIlyasDOMA/)

---

## ⭐ Поддержка проекта
Если проект оказался полезным, поставьте звезду на GitHub! 🌟
